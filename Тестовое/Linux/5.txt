# Сделайте символическую ссылку на файл access.log в папке html.

ln -s var/logs/access.log var/www/html/access_link.log # Создаю символическую ссылку на файл access.log в папке html, использую параметр -s для указания, что ссылка должна быть символической